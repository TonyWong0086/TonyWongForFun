(function(){'use strict';const projects=Array.from(document.querySelectorAll('.toy-card'));if(!projects.length)return;const button=document.createElement('button');button.className='surprise-button';button.type='button';button.textContent='↗ Surprise me';button.setAttribute('aria-label','Open a random toy');button.addEventListener('click',()=>{button.textContent='Choosing chaos…';button.disabled=true;const target=projects[Math.floor(Math.random()*projects.length)];window.setTimeout(()=>{window.location.href=target.href},280)});document.body.appendChild(button)})();
